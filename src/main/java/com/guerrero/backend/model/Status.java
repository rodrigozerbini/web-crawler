package com.guerrero.backend.model;

/**
 * Enumeration representing the status of a search result.
 */
public enum Status {
    /**
     * Indicates that the search is active and the results are partial.
     */
    active,
    /**
     * Indicates that the search is done and the results are complete.
     */
    done
}
